ptr1=fopen("aditya.txt","r");
    // ptr2=fopen("ad.txt","w");
    // c=fgetc(ptr1);
    // while(c!=EOF){   //EOF->end of file
    // fputc(c,ptr2);
    
    // c=fgetc(ptr1);
    // }
    
    
    // fclose(ptr1);
    // fclose(ptr2);